# https://taskfile.dev

version: '3'

tasks:
  default:
    deps:
      - task: fmt
      - task: check
  check:
    deps:
      - task: lint
    cmds:
      - renovate-config-validator default.json self.json
      - deno test --allow-read
  fmt:
    cmds:
      - dprint fmt
  lint:
    cmds:
      - dprint check
      - deno lint
      - typos . .github .vscode
